const weekday = new Date().getDay();
if (weekday == 1) {
    document.querySelector('.banner').style.display='block';
}

if (weekday == 2) {
    document.querySelector('.banner').style.display='block';
}
